# java-lotto-precourse

# 기능 요구 사항

로또 숫자의 범위는 1~45
1개의 로또 발행시 중복되지 않는 숫자 6개 뽑기.
당첨번호 추천시 중복되지 않는 숫자 6개와 보너스 번호 1개

1~5등까지
1- 20억 6개 일치
2- 3천만 5개 일치 + 보너스번호 1
3- 150만 5개 번호 일치
4- 5만 4개 번호 일치
5- 5천 3개 번호 일치

로또 구입 금액 입력시 구입 금액만큼 로또 발행
로또 1장의 가격은 1000
당첨 번호와 보너스 번호를 입력 -> 사용자가 입력

사용자가 구매한 로또 번호와 당첨번호 비교 후 당첨 내역 및 수익률 출력 로또 게임 종료
사용자가 잘못된 값 입력시 IllegalArgumentException 발생 시키고 ERROR 메세지 출력 후 재입력

Exception이 아닌 IllegalArgumentException, IllegalStateException 등 같은 명확한 유형을 처리
IllegalArguementException -> 전달된 파라미터가 잘못된 경우
IllegalStateException -> 객체의 상태가 현재 원하는 동작을 처리 불가능.

(오류 사항을 좀 더 세밀하게 처리)



구입금액을 입력해 주세요.
8000

8개를 구매했습니다.
[8, 21, 23, 41, 42, 43] 
[3, 5, 11, 16, 32, 38] 
[7, 11, 16, 35, 36, 44] 
[1, 8, 11, 31, 41, 42] 
[13, 14, 16, 38, 42, 45] 
[7, 11, 30, 40, 42, 43] 
[2, 13, 22, 32, 38, 45] 
[1, 3, 5, 14, 22, 45]

당첨 번호를 입력해 주세요.
1,2,3,4,5,6

보너스 번호를 입력해 주세요.
7

당첨 통계
---
3개 일치 (5,000원) - 1개
4개 일치 (50,000원) - 0개
5개 일치 (1,500,000원) - 0개
5개 일치, 보너스 볼 일치 (30,000,000원) - 0개
6개 일치 (2,000,000,000원) - 0개
총 수익률은 62.5%입니다.

# 프로그래밍 요구 사항 1
indent(인덴트, 들여쓰기) depth를 3이 넘지 않도록 구현한다. 2까지만 허용한다.
예를 들어 while문 안에 if문이 있으면 들여쓰기는 2이다.
힌트: indent(인덴트, 들여쓰기) depth를 줄이는 좋은 방법은 함수(또는 메서드)를 분리하면 된다.
3항 연산자를 쓰지 않는다.
함수(또는 메서드)가 한 가지 일만 하도록 최대한 작게 만들어라.
JUnit 5와 AssertJ를 이용하여 정리한 기능 목록이 정상적으로 작동하는지 테스트 코드로 확인한다.
테스트 도구 사용법이 익숙하지 않다면 아래 문서를 참고하여 학습한 후 테스트를 구현한다.
JUnit 5 User Guide
AssertJ User Guide
AssertJ Exception Assertions
Guide to JUnit 5 Parameterized Tests

# 프로그래밍 요구 사항 2
함수(또는 메서드)의 길이가 15라인을 넘어가지 않도록 구현한다.
함수(또는 메서드)가 한 가지 일만 잘 하도록 구현한다.
else 예약어를 쓰지 않는다.
else를 쓰지 말라고 하니 switch/case로 구현하는 경우가 있는데 switch/case도 허용하지 않는다.
힌트: if 조건절에서 값을 return하는 방식으로 구현하면 else를 사용하지 않아도 된다.
Java Enum을 적용하여 프로그램을 구현한다.
구현한 기능에 대한 단위 테스트를 작성한다. 단, UI(System.out, System.in, Scanner) 로직은 제외한다.
단위 테스트 작성이 익숙하지 않다면 LottoTest를 참고하여 학습한 후 테스트를 작성한다.
라이브러리

# Lotto 클래스
제공된 Lotto 클래스를 사용하여 구현해야 한다.
Lotto에 numbers 이외의 필드(인스턴스 변수)를 추가할 수 없다.
numbers의 접근 제어자인 private은 변경할 수 없다.
Lotto의 패키지를 변경할 수 있다.

# Read me
1. 초기 기능 작성
입력 받은 돈 1000원당 1장으로 변환하는 메서드 작성
로또 한 장당 1000원 기준으로 구매 수량을 계산하는 기본 로직을 구현했습니다.

돈 입력받는 메서드 getMoney 작성
사용자로부터 투자 금액을 입력받는 메서드를 작성했습니다.

2. 기능 확장 및 로직 구체화
로또 번호 뽑는 로직 및 적립금 로직 추가
로또 번호 랜덤 추첨과 각 항목별 번호 및 금액 출력 기능을 추가했습니다.

Integer형 입력값을 문자열로 변환하여 처리하는 로직 구현
입력값 타입에 따른 예외를 방지하기 위해 변환 및 검증 로직을 추가했습니다.

lotto 클래스를 사용하여 검증 로직 및 리스트 반환 메서드 추가
데이터 구조를 정리하고, 로또 번호 리스트를 효율적으로 관리하도록 개선했습니다.

3. 검증 및 예외 처리 강화
enum을 활용한 돈 검증 및 예외 처리 추가
돈 검증에 enum을 도입해 코드의 일관성과 안정성을 높였습니다.

결과 계산 로직 및 enum 기반 생성자/필드 추가
enum을 이용한 당첨 결과 계산 로직을 추가하고, 로또 결과를 Systematic하게 관리했습니다.

Validate 부분 검증 로직 추가
입력값 검증 로직을 더욱 세밀하게 조정했습니다.

4. 테스트 및 버그 수정
ApplicationTest ERROR 출력 부분 수정
예외 메시지 출력 관련 테스트를 수정하고 출력 형식을 개선했습니다.

5. 예외 처리 일원화 및 메인 구조 개선
메인 Application에 직접 try-catch로 처리
프로그램 전체 흐름에서 발생할 수 있는 예외를 메인 Application에서 통합적으로 처리하도록 설계했습니다.
잘못된 입력 시 프로그램이 예측 가능한 방식으로 종료되며, 사용자에게 명확한 에러 메시지를 제공합니다.
